# Dados locais (não commitados): path do repo para sourceDir
.chezmoidata.toml
.chezmoidata/

# Material sensível que precisa morar perto do repo mas nunca ser publicado —
# este repo é público. Certificado de CA interna, kubeconfig, fstab da máquina.
# Ver docs/artifacts.md.
artifacts/

# Claude Code runtime/local
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Playwright MCP session logs
/.playwright-mcp/

# Stray runtime junk
debug.log
__pycache__/
*.pyc
