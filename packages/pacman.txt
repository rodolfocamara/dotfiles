# Arch Linux packages (one per line; # comments ignored)
#
# Esta lista é a base: vale em qualquer máquina Arch, seja qual for o desktop.
# O que é de um desktop só mora em packages/pacman.<profile>.txt — o
# install-packages.linux-arch.sh resolve o perfil e concatena as duas.
# Ver docs/machine-profiles.md.
base-devel
git
chezmoi
zsh
zsh-autosuggestions
starship
fzf
zoxide
ripgrep
fd
bat
eza
tmux
neovim
wl-clipboard
wayland-protocols
imagemagick
python
# uv: gerencia as venvs e os interpretadores dos projetos Python. Sem ele, um
# `.venv` criado em outra máquina fica com o symlink do interpretador pendurado
# e nada roda até refazer com `uv sync`.
uv
sqlite
pipewire
wireplumber
polkit-kde-agent
noto-fonts
noto-fonts-emoji
ttf-nerd-fonts-symbols

# Containers rootless (SearxNG via quadlet — ver docs/searxng.md)
podman

# Telas externas na NVIDIA — ver docs/nvidia-displays.md
# jq: a função fix-monitor lê o `kscreen-doctor --json`
# ddcutil: separa "EDID lê" de "monitor acordado", que é o diagnóstico inteiro
jq
ddcutil
